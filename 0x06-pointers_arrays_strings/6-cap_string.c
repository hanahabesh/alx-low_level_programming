#include "main.h"
/**
 * _string - function to capitalize every single word in string
 * @str: the string
 * Return: return capitalized word
 */
char *cap_string(char *)
{
	int i;

	if (a[i] <= 'a' && 
