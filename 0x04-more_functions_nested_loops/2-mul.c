#include "main.h"

/**
 * mul - multiply two integers
 * @a: first number
 * @b: second number
 * Return: return the multiple result
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
